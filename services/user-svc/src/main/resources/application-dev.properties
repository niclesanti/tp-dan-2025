spring.application.name=user-svc

# ── springdoc-openapi ─────────────────────────────────────────────
springdoc.swagger-ui.path=/swagger-ui
springdoc.swagger-ui.operations-sorter=method
springdoc.swagger-ui.enabled=true
springdoc.api-docs.path=/openapi.json
springdoc.api-docs.enabled=true

# ── Configuración MySQL (Development) ──────────────────────────────
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:mysql}:3306/${MYSQL_DATABASE:users}?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=${MYSQL_USER:usr_app}
spring.datasource.password=${MYSQL_PASSWORD:usrapp}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ── Configuración Hikari (Development - más logs) ──────────────────
spring.datasource.hikari.minimumIdle=2
spring.datasource.hikari.maximumPoolSize=8
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.leak-detection-threshold=60000

# ── Configuración Hibernate (Development - verbose) ────────────────
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.use_sql_comments=true

# ── Logging (Development) ──────────────────────────────────────────
logging.level.root=INFO
logging.level.edu.utn.frsf.isi.dan=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# ── Actuator (Development - todos los endpoints expuestos) ────────
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always
management.endpoints.enabled-by-default=true
management.endpoint.shutdown.enabled=true

# ── Server Configuration ───────────────────────────────────────────
server.port=8080
server.error.include-message=always
server.error.include-stacktrace=always
